# Metro (React Native) ne suit pas les symlinks pnpm → node_modules aplati requis pour apps/mobile
# Impact global monorepo (web + api) — non-régression validée dans MOB-1.1 (AC #2)
node-linker=hoisted

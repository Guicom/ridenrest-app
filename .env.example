# ============================================================
# Docker Compose — Infrastructure services (db + redis)
# Copy this file to .env and adjust for your local environment
# ============================================================

# --- PostgreSQL (Docker container) ---
POSTGRES_USER=ridenrest
POSTGRES_PASSWORD=ridenrest
POSTGRES_DB=ridenrest

# --- Caddy / VPS (production only) ---
# Uncomment on VPS. Let's Encrypt uses this for certificate renewal notifications.
# ACME_EMAIL=your@email.com

# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
.next
.turbo
coverage

# Environment files
.env
.env.*
.env.local
.env.*.local

# Vercel
.vercel

# OS
.DS_Store

# Editor
.vscode
.idea

# User-uploaded files (never commit)
apps/api/uploads/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Agent worktrees (ephemeral, never commit)
.claude/worktrees/
# Lock runtime du MCP scheduled-tasks (artefact local)
.claude/scheduled_tasks.lock

# Expo : cache/artefacts locaux à la racine (logs xcodebuild, prebuild, devices…)
.expo/
